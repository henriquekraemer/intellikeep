// IntelliKeep Panel — placeholder
// Run `npm run build` inside panel/ to generate the production bundle.
console.warn(
  "IntelliKeep: intellikeep-panel.js is a placeholder. " +
  "Run `npm run build` inside the panel/ directory to build the panel."
);
