// IntelliKeep Lovelace Card — placeholder
// Run `npm run build` inside lovelace-card/ to generate the production bundle.
console.warn(
  "IntelliKeep: intellikeep-card.js is a placeholder. " +
  "Run `npm run build` inside the lovelace-card/ directory to build the card."
);
